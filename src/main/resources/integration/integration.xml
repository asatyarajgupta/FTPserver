<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:file="http://www.springframework.org/schema/integration/file"
       xmlns:feed="http://www.springframework.org/schema/integration/feed"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/integration/feed https://www.springframework.org/schema/integration/feed/spring-integration-feed.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration/file https://www.springframework.org/schema/integration/file/spring-integration-file.xsd
		http://www.springframework.org/schema/integration https://www.springframework.org/schema/integration/spring-integration.xsd">

    <!-- 1. The source - reads files from directory -->
    <bean id="pollableFileSource"
          class="org.springframework.integration.file.inbound.FileReadingMessageSource"
          p:directory="D:/Games"/>

    <!-- 2. A poller that triggers it every 5 seconds -->
    <int:inbound-channel-adapter ref="pollableFileSource" channel="fileChannel">
        <int:poller fixed-rate="5000"/>
    </int:inbound-channel-adapter>

    <!-- 3. A channel to pass messages through -->
    <int:channel id="fileChannel"/>

    <!-- 4. Output - prints to console -->
    <int:logging-channel-adapter channel="fileChannel" log-full-message="true"/>
</beans>